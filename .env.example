# Database
DATABASE_URL="postgresql://user:password@localhost:5432/sales_scheduler"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-a-secure-random-secret"

# Google OAuth
GOOGLE_CLIENT_ID="random"
GOOGLE_CLIENT_SECRET="randon"

# Google Calendar API (same credentials as OAuth, but ensure Calendar API is enabled)
# Use the same GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET

# Optional: Resend for email (e.g. meeting confirmations) - 3,000/month free
# RESEND_API_KEY="re_..."
# RESEND_FROM_EMAIL="noreply@yourdomain.com"
